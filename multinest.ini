//MultiNest Configuration file
0                  // do mode separation?
0                  // run in constant efficiency mode?
4000               // number of live points
1                  // need feedback to standard output (terminal)?
0                  // resume from a previous job? (only available if asimov data being used)
0.3                // set the required efficiency. 0.8 and 0.3 are recommended for parameter estimation & evidence evalutaion respectively.
0.05                // tol, defines the stopping criteria, 0.5 gives good enough accuracy
-1.e90             // all the modes with logZ < Ztol are ignored
